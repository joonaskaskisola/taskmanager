{% block formdata %}
    {{ form_row(form.customer) }}

    {{ include('fields/text.html.twig', {field: form.firstName}, with_context = false) }}
    {{ include('fields/text.html.twig', {field: form.lastName}, with_context = false) }}
    {{ include('fields/text.html.twig', {field: form.username}, with_context = false) }}
    {{ include('fields/text.html.twig', {field: form.email}, with_context = false) }}
    {{ include('fields/text.html.twig', {field: form.password}, with_context = false) }}
    {{ include('fields/text.html.twig', {field: form.phone}, with_context = false) }}

    {{ form_row(form.enabled) }}
    {{ form_row(form.country) }}

{% endblock %}
