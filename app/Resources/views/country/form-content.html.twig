{% block formdata %}

    {{ include('fields/text.html.twig', {field: form.name}, with_context = false) }}
    {{ include('fields/text.html.twig', {field: form.code}, with_context = false) }}
    {{ include('fields/text.html.twig', {field: form.langCode}, with_context = false) }}

{% endblock %}
