{% block formdata %}

    {{ include('fields/text.html.twig', {field: form.name}, with_context = false) }}

    {{ form_row(form.category) }}
    {{ form_row(form.unit) }}

    {{ include('fields/numeric.html.twig', {field: form.price}, with_context = false) }}

{% endblock %}
