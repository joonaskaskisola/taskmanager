{% block formdata %}

    {{ include('fields/text.html.twig', {field: form.name}, with_context = false) }}
    {{ include('fields/text.html.twig', {field: form.name2}, with_context = false) }}
    {{ include('fields/text.html.twig', {field: form.email}, with_context = false) }}
    {{ include('fields/text.html.twig', {field: form.businessId}, with_context = false) }}
    {{ include('fields/text.html.twig', {field: form.streetAddress}, with_context = false) }}
    {{ include('fields/text.html.twig', {field: form.zipCode}, with_context = false) }}
    {{ include('fields/text.html.twig', {field: form.locality}, with_context = false) }}
    {{ include('fields/text.html.twig', {field: form.country}, with_context = false) }}


{% endblock %}
